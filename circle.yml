machine:
    python:
        version: 3.6.0

dependencies:
    post:
        - pip install -r py-requirements/dev.txt

deployment:
    production:
        branch: master
        heroku:
            appname: fc-firebot
